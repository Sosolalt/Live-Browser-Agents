{
  "name": "gemini-hackaton-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @gemini-hackaton/orchestrator && npm run build -w @gemini-hackaton/backend && npm run build -w @gemini-hackaton/extension",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
